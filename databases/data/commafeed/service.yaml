apiVersion: v1
kind: Service
metadata:
  name: commafeed-db
spec:
  selector:
    # app: postgres
    app.kubernetes.io/name: commafeed-db
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 5432
      protocol: TCP
      targetPort: 5432
