apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  type: ClusterIP
  # clusterIP: None
  ports:
    - port: 5432
      protocol: TCP
      targetPort: 5432
