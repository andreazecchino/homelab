apiVersion: v1
kind: Service
metadata:
  name: linkding
  labels:
    app: linkding
    app.kubernetes.io/name: linkding
    app.kubernetes.io/component: svc
    app.kubernetes.io/part-of: linkding
spec:
  selector:
    app: linkding
  type: ClusterIP
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
