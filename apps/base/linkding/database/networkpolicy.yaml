apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: linkding-db-ingress
  namespace: linkding
spec:
  description: "Database only accepts ingress from its own app, denies everything else"
  endpointSelector:
    matchLabels:
      cnpg.io/cluster: linkding-db
  ingress:
    - fromEndpoints:
        - matchLabels:
            app: linkding
      toPorts:
        - ports:
            - port: "5432"
              protocol: TCP
