apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: commafeed
  labels:
    app: commafeed
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 256Mi
