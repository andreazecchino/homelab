apiVersion: v1
kind: Service
metadata:
  name: commafeed-svc
  labels:
    app: commafeed
spec:
  selector:
    app: commafeed
  type: NodePort
  ports:
    - port: 8082
      protocol: TCP
      targetPort: 8082
